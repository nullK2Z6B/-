#include <stdio.h>
int main()
{
	char str1[25] = "wo ai de ren bu ai wo";   //定义字符数组，并用字符串常量为其赋初值
	char str2[25] = "so sad a story it is.";
	printf("%s, %s, str1, str2");              //打印字符串用%s
	return 0;
}